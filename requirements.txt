# LangGraph and LLM
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# Observability (optional): visualize runs in LangSmith
langsmith>=0.2.0

# Web search for latest data (optional)
langchain-tavily>=0.2.0

# NSE data (free)
nse[local]>=2.1.0

# Financials (Indian stocks; finfetch pulls yfinance, lxml, beautifulsoup4, requests, etc.)
yfinance>=0.2.0
finfetch>=0.2.0
lxml>=5.0.0
beautifulsoup4>=4.12.0
requests>=2.28.0

# PDF report: headless browser (best fidelity) > WeasyPrint > ReportLab fallback
playwright>=1.40.0
weasyprint>=62.0
reportlab>=4.0.0
Jinja2>=3.1.0
markdown>=3.5.0

# PDF text extraction (for concall transcript parsing)
pypdf>=4.0.0

# Tests
pytest>=7.4.0

# Config and types
python-dotenv>=1.0.0
pydantic>=2.0.0

# Backend API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# PostgreSQL
psycopg2-binary>=2.9.0

# Auth (Google OAuth2 + JWT)
authlib>=1.3.0
httpx>=0.27.0
python-jose[cryptography]>=3.3.0
